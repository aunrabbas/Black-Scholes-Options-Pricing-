# Black-Scholes-Options-Pricing-
I created a calculator that uses the black scholes formula, and outputs a heatmap for both calls and puts. Allows for input on variable and showcases a range between those variables on the heatmap. 
